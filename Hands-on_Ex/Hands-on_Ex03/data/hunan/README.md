# Hunan Validation Data

This copy comes from `data3.zip` and is retained as a local validation package for the later spatial-weights workflow.

The active Exercise 3 page uses the Bangka-Belitung MODIS fire data in `data/rawdata/`. The Hunan files remain in this separate folder so the fire case study keeps its original data and reproducible results.
